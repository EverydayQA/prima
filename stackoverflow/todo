# a tool for the stackoverflow toolbox
4 spaces for code
url
> block on each line - no line break or need to 2 lines break

edit OP causing more attentions

perl or python
extention .stk - a script to clean up before commit

# a tool to extract code from the page interested
# instead of copy paste - and losing format
copy code from html - paster to text, use nano
why vim cannot keep original format?
:set paste
:set nopaste
