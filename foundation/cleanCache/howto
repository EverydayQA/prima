
gliang Nov 18, 2015

# do not forget - priority - show testing


# create a cpan package with best perl practice
# module-starter - a perl cpan package
# 1 lib.pm only
 module-starter --module cleanCache --author="Gang Liang" --email="gang.liang.2011@gmail.com"

perl Makefile.PL
make
make test
# generate test coverage of the package
cover -test

# tar gz the package with proper version
# create tar.gz file with proper version
make dist

# create a rpm spec file - have to modify a little
# cpanspec came with cpan2rpm? 
cpanspec cleanCache-1.1.tar.gz

# no actual coding, but everything is there

